name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm format:check
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm --filter @bugping/widget build
      - run: pnpm --filter @bugping/widget size
      - run: pnpm --filter @bugping/widget check:bundle
      - run: pnpm --filter @bugping/web build

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @bugping/widget exec playwright install --with-deps chromium
      - run: pnpm --filter @bugping/widget e2e
      - run: pnpm --filter @bugping/web exec playwright install --with-deps chromium
      - run: pnpm --filter @bugping/web e2e
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: widget-e2e-report
          path: |
            packages/widget/playwright-report
            packages/widget/test-results
            apps/web/playwright-report
            apps/web/test-results

  db-supabase:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm db:start
      - run: pnpm db:test
        env:
          DB_TEST_TARGET: supabase
      - run: pnpm --filter @bugping/web test
        env:
          DB_TEST_TARGET: supabase
